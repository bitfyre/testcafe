#!/usr/bin/env bash

echo "[i] Starting docker for development"

docker run \
  --interactive \
  --init \
  --tty \
  --mount type=bind,src="$(pwd)",dst="/test" \
  -p 8881:8881 \
  -p 8882:8882 \
  testcafe/testcafe \
  remote /test/test.js --ports 8881,8882 --hostname localhost -e

